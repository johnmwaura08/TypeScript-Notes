//arrays

let names = ['juliette', 'jennifer', 'katelyn'];

names.push('steve')

// when we declare an array, the types that we assigned cant be changed later on

// we can have a mixed array if we declare it to have mixed types 


//objects 


let ninja = {

    name : 'jon',
    belt: 'blue',
    age: 22
};

ninja.age = 40 // this is fine
// ninja.age = '44' // this is an error because the type is arleady a number 
